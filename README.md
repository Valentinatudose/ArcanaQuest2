# ArcanaQuest — Tarot Learning App

An educational app that helps users master Tarot card meanings through interactive draws, study, and adaptive quizzes.

- Built with React + Vite + TypeScript
- Deployed on Vercel
- Collects user emails securely via Brevo

## To run locally:
1. `npm install`
2. Set `BREVO_API_KEY` in Vercel (not in code)
3. `npm run dev`